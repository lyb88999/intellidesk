spring:
  application:
    name: intellidesk-chat-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yml
        refresh-enabled: true
      discovery:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP
