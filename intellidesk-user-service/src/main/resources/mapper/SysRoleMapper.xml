<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.intellidesk.user.mapper.SysRoleMapper">

    <!-- 根据角色ID查询权限编码列表 -->
    <select id="selectPermissionCodesByRoleId" resultType="java.lang.String">
        SELECT sp.permission_code
        FROM sys_permission sp
                 INNER JOIN sys_role_permission srp ON sp.id = srp.permission_id
        WHERE srp.role_id = #{roleId}
          AND sp.deleted = 0
          AND sp.status = 1
    </select>

</mapper>
